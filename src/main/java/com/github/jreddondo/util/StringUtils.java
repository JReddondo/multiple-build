package com.github.jreddondo.util;

/**
 * Utility class for string operations and constants.
 */
public final class StringUtils {

    /**
     * Separator line with 80 equals signs.
     */
    public static final String SEPARATOR_LINE = "=".repeat(80);
    /**
     * Dash line with 80 dashes.
     */
    public static final String DASH_LINE = "-".repeat(80);

    private StringUtils() {
        // Utility class, prevent instantiation
    }
}

